from __future__ import annotations

# Slot vocab / claim space schema version.
#
# Any change to the bounded vocabularies in `provetok.pcg.schema` MUST bump this
# version string and update `docs/schema_v1.md` (or successor docs).
SCHEMA_VERSION = "slot_vocab_v1.0"

# Documentation path (repo-relative).
SCHEMA_DOC = "docs/schema_v1.md"

