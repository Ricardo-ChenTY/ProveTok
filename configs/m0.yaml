seed: 0
device: auto  # auto|cpu|cuda
run_name: m0_sanity

data:
  type: synthetic
  num_samples: 64
  vol_shape: [64, 128, 128]

model:
  emb_dim: 32
  cite_topk: 3

train:
  epochs: 1
  batch_size: 4
  lr: 1.0e-3
  weight_decay: 0.0
  log_every: 10
  save_dir: checkpoints
