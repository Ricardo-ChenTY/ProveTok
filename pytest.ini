[pytest]
# This repo contains archived legacy materials under docs/legacy/, scripts/legacy/, tests_legacy/.
# They are kept for reference but should not participate in current test runs.
norecursedirs =
    docs/legacy
    scripts/legacy
    tests_legacy
    outputs
    .rd_queue
    node_modules
